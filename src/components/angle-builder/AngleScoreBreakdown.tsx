// src/components/angle-builder/AngleScoreBreakdown.tsx
"use client";

export function AngleScoreBreakdown() {
  return (
    <div className="glass-card rounded-2xl border border-slate-800/60 bg-slate-900/40 p-5">
      <h3 className="text-base font-semibold text-white">Angle Score Breakdown</h3>
      <p className="mt-4 text-xs text-slate-500">Angle scoring isn&apos;t available yet — no scoring model is wired to angle generation.</p>
    </div>
  );
}